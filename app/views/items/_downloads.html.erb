
<table class="table table-striped table-condensed">
  <thead><td>IP</td><td>date</td></thead>
  <% impressions.each do |imp| %>

      <tr>
        <td><%= imp.ip_address %></td>
        <td><%= imp.created_at %></td>
      </tr>

  <% end %>
</table>